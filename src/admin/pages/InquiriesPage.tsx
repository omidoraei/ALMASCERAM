import { useEffect, useMemo, useState } from 'react'
import { ArrowLeft, CheckCircle, ClipboardText, MagnifyingGlass } from '@phosphor-icons/react'
import { Dialog, DialogContent, DialogDescription, DialogTitle } from '../../components/ui/dialog'
import { getAdminInquiries, updateInquiryStatus } from '../../lib/actions/admin-actions'
import { getInquiryItems } from '../../lib/actions/inquiry-detail-actions'
import { isSupabaseConfigured } from '../../lib/supabase/client'
import type { InquiryAdminStatus } from '../../lib/validation/admin-catalog'

type Inquiry = { id: string; number: string; customer: string; company: string; city: string; itemsCount: number; status: InquiryAdminStatus; submittedAt: string }
type Item = { id: string; product: string; sku: string; size: string; quantity: number; requestedSqm: number | null }
const statuses: Record<InquiryAdminStatus, string> = { submitted: 'جدید', in_review: 'در حال بررسی', quoted: 'پاسخ داده‌شده', closed: 'بسته‌شده', cancelled: 'لغوشده' }
const demo: Inquiry[] = [{ id: 'd1', number: '۸۲۱۶', customer: 'آرمان رضایی', company: 'استودیو بُن', city: 'تهران', itemsCount: 4, status: 'submitted', submittedAt: 'امروز، ۱۰:۲۸' }, { id: 'd2', number: '۸۲۱۵', customer: 'سارا توسلی', company: 'معماران رواق', city: 'شیراز', itemsCount: 2, status: 'in_review', submittedAt: 'امروز، ۰۹:۱۲' }, { id: 'd3', number: '۸۲۱۴', customer: 'پویان منصوری', company: 'ساختمان آرتا', city: 'اصفهان', itemsCount: 6, status: 'quoted', submittedAt: 'دیروز' }]

export function InquiriesPage() {
  const [rows, setRows] = useState<Inquiry[]>(demo)
  const [search, setSearch] = useState('')
  const [status, setStatus] = useState<'all' | InquiryAdminStatus>('all')
  const [selected, setSelected] = useState<Inquiry | null>(null)
  const [items, setItems] = useState<Item[]>([])
  const [loadingItems, setLoadingItems] = useState(false)
  const [message, setMessage] = useState('')
  const load = async () => { if (!isSupabaseConfigured) return; const data = await getAdminInquiries(); setRows((data as unknown as Array<{ id: string; inquiry_number: number; customer_name: string | null; company_name: string | null; project_city: string | null; status: InquiryAdminStatus; submitted_at: string; inquiry_items: Array<{ count: number }> }>).map((item) => ({ id: item.id, number: item.inquiry_number.toLocaleString('fa-IR'), customer: item.customer_name ?? 'مشتری', company: item.company_name ?? '—', city: item.project_city ?? '—', itemsCount: item.inquiry_items[0]?.count ?? 0, status: item.status, submittedAt: new Date(item.submitted_at).toLocaleString('fa-IR') }))) }
  useEffect(() => { const timer = window.setTimeout(() => void load(), 0); return () => window.clearTimeout(timer) }, [])
  const filtered = useMemo(() => rows.filter((row) => (status === 'all' || row.status === status) && `${row.number} ${row.customer} ${row.company} ${row.city}`.includes(search)), [rows, search, status])
  const change = async (row: Inquiry, next: InquiryAdminStatus) => { if (isSupabaseConfigured) await updateInquiryStatus(row.id, next); setRows((items) => items.map((item) => item.id === row.id ? { ...item, status: next } : item)); setSelected((item) => item?.id === row.id ? { ...item, status: next } : item); setMessage('وضعیت استعلام به‌روزرسانی شد') }
  const open = async (row: Inquiry) => { setSelected(row); setLoadingItems(true); if (!isSupabaseConfigured) { setItems([{ id: 'i1', product: 'آرنا سند', sku: 'KR-0101', size: '۱۲۰ × ۲۴۰ × ۹', quantity: 2, requestedSqm: 180 }, { id: 'i2', product: 'کلکته اورو', sku: 'KR-0103', size: '۸۰ × ۱۶۰ × ۹', quantity: 1, requestedSqm: 75 }]); setLoadingItems(false); return } try { const data = await getInquiryItems(row.id); setItems((data as unknown as Array<{ id: string; quantity: number; requested_sqm: number | null; sizes: { width_mm: number; height_mm: number; thickness_mm: number; products: { name: string; sku: string } | null } | null }>).map((item) => ({ id: item.id, product: item.sizes?.products?.name ?? 'محصول', sku: item.sizes?.products?.sku ?? '—', size: item.sizes ? `${item.sizes.width_mm / 10} × ${item.sizes.height_mm / 10} × ${item.sizes.thickness_mm}` : '—', quantity: item.quantity, requestedSqm: item.requested_sqm }))) } finally { setLoadingItems(false) } }

  return <section className="module-page"><header className="module-page-head"><div><small>Lead Management</small><h2>مدیریت استعلام‌ها</h2><p>بررسی مشتری، اقلام انتخاب‌شده و چرخه پاسخ‌گویی</p></div><span className="seo-health"><ClipboardText size={17} /> {rows.filter((item) => item.status === 'submitted').length.toLocaleString('fa-IR')} مورد جدید</span></header>{message && <div className="module-success"><CheckCircle size={16} />{message}<button onClick={() => setMessage('')}>×</button></div>}<div className="module-table-card"><div className="module-toolbar inquiries"><label><MagnifyingGlass size={17} /><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="کد، مشتری، شرکت یا شهر..." /></label><select value={status} onChange={(event) => setStatus(event.target.value as typeof status)}><option value="all">همه وضعیت‌ها</option>{Object.entries(statuses).map(([value, text]) => <option key={value} value={value}>{text}</option>)}</select></div><div className="admin-table-wrap"><table className="admin-data-table"><thead><tr><th>کد</th><th>مشتری / شرکت</th><th>شهر</th><th>اقلام</th><th>زمان ثبت</th><th>وضعیت</th><th /></tr></thead><tbody>{filtered.map((row) => <tr key={row.id}><td><b>#{row.number}</b></td><td><div className="customer-cell"><span>{row.customer[0]}</span><p><b>{row.customer}</b><small>{row.company}</small></p></div></td><td>{row.city}</td><td>{row.itemsCount.toLocaleString('fa-IR')} قلم</td><td>{row.submittedAt}</td><td><select className="status-select review" value={row.status} onChange={(event) => void change(row, event.target.value as InquiryAdminStatus)}>{Object.entries(statuses).map(([value, text]) => <option key={value} value={value}>{text}</option>)}</select></td><td><button className="table-view-button" onClick={() => void open(row)}>مشاهده <ArrowLeft size={14} /></button></td></tr>)}</tbody></table></div></div><Dialog open={selected !== null} onOpenChange={(next) => !next && setSelected(null)}>{selected && <DialogContent className="inquiry-detail-dialog" dir="rtl"><DialogTitle>استعلام #{selected.number}</DialogTitle><DialogDescription>{selected.customer} · {selected.company} · {selected.city}</DialogDescription><div className="inquiry-detail-summary"><span>تعداد اقلام<b>{selected.itemsCount.toLocaleString('fa-IR')}</b></span><span>تاریخ ثبت<b>{selected.submittedAt}</b></span><label>وضعیت<select value={selected.status} onChange={(event) => void change(selected, event.target.value as InquiryAdminStatus)}>{Object.entries(statuses).map(([value, text]) => <option key={value} value={value}>{text}</option>)}</select></label></div><h3>اقلام انتخاب‌شده</h3>{loadingItems ? <div className="module-empty">در حال دریافت اقلام...</div> : <div className="inquiry-item-list">{items.map((item) => <article key={item.id}><span>{item.product[0]}</span><p><b>{item.product}</b><small>{item.sku} · {item.size} میلی‌متر</small></p><div><b>{item.quantity.toLocaleString('fa-IR')} عدد</b><small>{item.requestedSqm ? `${item.requestedSqm.toLocaleString('fa-IR')} مترمربع` : 'متراژ ثبت نشده'}</small></div></article>)}</div>}</DialogContent>}</Dialog></section>
}
